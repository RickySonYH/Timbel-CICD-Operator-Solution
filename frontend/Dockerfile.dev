# [advice from AI] Frontend 개발용 Dockerfile - 안정성 향상
# React 18 + TypeScript + Material-UI 개발 환경

FROM node:20-alpine

# 작업 디렉토리 설정
WORKDIR /app

# 시스템 패키지 업데이트 및 필수 도구 설치
RUN apk update && apk add --no-cache \
    git \
    curl \
    bash

# 패키지 파일 복사 (캐시 최적화)
COPY package*.json ./

# 의존성 설치 (더 안정적인 설치)
RUN npm ci

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 3000

# 헬스체크 추가
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD curl -f http://localhost:3000 || exit 1

# [advice from AI] 환경 변수 설정 - docker-compose.yml에서 덮어쓸 수 있도록 기본값만 설정
ENV CHOKIDAR_USEPOLLING=true
ENV WATCHPACK_POLLING=true
ENV NODE_ENV=development
ENV DISABLE_ESLINT_PLUGIN=true
ENV GENERATE_SOURCEMAP=false

# 개발 서버 실행 (더 안정적인 시작)
CMD ["sh", "-c", "npm start"]
